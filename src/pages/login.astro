---
export const prerender = false;
import AuthLayout from '@layouts/AuthLayout.astro';
import AuthCard from '@components/AuthCard.astro';
import AuthSidePanel from '@components/AuthSidePanel.astro';
import { SignIn } from '@clerk/astro/components';
import { clerkAppearance } from '@lib/clerk-appearance';
import { HTTP_CONFIG, ROUTES } from '@/config/index.config';

const title = 'Ingresar';
Astro.response.headers.set('Cache-Control', HTTP_CONFIG.cacheControl.private);
---

<AuthLayout {title} actionHref={ROUTES.register} actionLabel="Crear cuenta">
  <AuthSidePanel slot="side" variant="login" />
  <AuthSidePanel slot="mobile" variant="login" compact />

  <AuthCard>
    <span slot="stamp">AUTH · 2026</span>
    <span slot="heading">Bienvenido de vuelta</span>
    <span slot="kicker">/01 · login</span>

    <SignIn
      appearance={clerkAppearance}
      withSignUp={false}
      signUpUrl={ROUTES.register}
      fallbackRedirectUrl={ROUTES.account}
      oauthFlow="auto"
    />

    <div slot="footer" class="contents">
      <span>¿Sin cuenta? <a href={ROUTES.register}>Creá una en /registro</a></span>
      <span>¿Olvidaste tu clave? <a href={ROUTES.resetPassword}>Recuperar por email</a></span>
    </div>
  </AuthCard>

  <span slot="disclaimer">Prototipo · Credenciales gestionadas de forma segura por Clerk</span>
</AuthLayout>
